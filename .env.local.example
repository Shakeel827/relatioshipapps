# Frontend Environment Setup

Copy this file to `.env.local` and update values for your local development.

## Local Development

```
EXPO_PUBLIC_API_URL=http://localhost:3000/api
EXPO_PUBLIC_APP_NAME=Relastin
EXPO_PUBLIC_APP_VERSION=1.0.0
EXPO_PUBLIC_ENABLE_ANALYTICS=false
EXPO_PUBLIC_ENABLE_CRASH_REPORTING=false
```

## Staging

```
EXPO_PUBLIC_API_URL=https://api-staging.relastin.app/api
EXPO_PUBLIC_APP_NAME=Relastin (Staging)
EXPO_PUBLIC_APP_VERSION=1.0.0
EXPO_PUBLIC_ENABLE_ANALYTICS=true
EXPO_PUBLIC_ENABLE_CRASH_REPORTING=true
```

## Production

```
EXPO_PUBLIC_API_URL=https://api.relastin.app/api
EXPO_PUBLIC_APP_NAME=Relastin
EXPO_PUBLIC_APP_VERSION=1.0.0
EXPO_PUBLIC_ENABLE_ANALYTICS=true
EXPO_PUBLIC_ENABLE_CRASH_REPORTING=true
```

## Notes

- `EXPO_PUBLIC_*` variables are bundled into the app (safe for public)
- Update `EXPO_PUBLIC_API_URL` to your backend server
- Never commit `.env.local` to version control
- Analytics disabled in development to avoid noise
